Options -Indexes

<FilesMatch "^(config\.php|callback\.php|login\.php|videos\.php)$">
    <IfModule mod_authz_core.c>
        Require all granted
    </IfModule>
</FilesMatch>

<FilesMatch "^(README\.txt)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
</FilesMatch>
